let altura_cm= 178
let altura_m=1.78
let peso_kg= 78.4
let alturaRound =Math.round(altura_m)
let pesoFloor =Math.floor(peso_kg)
let booleano = (Number.MAX_VALUE+1 === Number.MAX_VALUE) 
console.log(booleano);